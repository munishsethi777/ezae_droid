<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="in.learntech.rights.SendMessage"
    tools:showIn="@layout/activity_send_message">
    <LinearLayout
        android:id="@+id/relativeLayout"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent">
        <TextView
            android:id="@+id/searchLearners"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:text="Search Learners"
            android:padding="10sp"
            android:layout_weight="1"/>
        <ListView
            android:id="@+id/learnersList"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_below="@id/searchLearners"
            android:layout_weight="10">
        </ListView>
    </LinearLayout>

</android.support.constraint.ConstraintLayout>
